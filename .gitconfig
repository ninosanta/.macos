[user]
	name = ninosanta
	email = 38584348+ninosanta@users.noreply.github.com
[core]
	excludesfile = ~/.gitignore_global
[credential]
	helper = store --file ~/.git-credentials
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
